name: Build and publishid Image Docker
on:
  push: 
    branches:
      - main
      - dev
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}
      - name: Build image Docker
        run: docker build -t ${{ secrets.DOCKER_USER }}/final-ps-iii:latest .
      - name: Publishid iso docker
        run: docker push ${{ secrets.DOCKER_USER }}/final-ps-iii:latest

      - name: Enviar resultado por correo
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.EMAIL_USER }}
          password: ${{ secrets.EMAIL_PASS }}
          subject: "Despliegue completado"
          to: "servar813@gmail.com"
          from: "GitHub Actions"
          body: |
            ✅ El despliegue fue exitoso.
          


